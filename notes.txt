next: 
[] handle server response
[] Support client multi message.
[] Support client sending bad message.
[] Support release build where you check unsued var.
[] Even if parse is good, must fail on extra bytes...
[x] Pipeline stage 1: send all data in format [len][data]
[x]  Pipeline stage 2: Parse all data in format
if type == Set
[type:len:data:len:data]
if type == Get
[type:len:data]
[x] move Str to Buffer
[] need to handle the case of read only string vs malloc. (perhaps make all malloc?)
[] not a fan of you set_state_writing resets bytes processed
[] install redis to compare against, not now, try to use your head first.

Memory bugs:
Use after free (could be freed by free, or auto freed by the stack)
When you malloc the wrong size...like the pointer instead of what the pointer points to...

Pipeline Server:
1. Read all data for one request
2. Parse data

Future features:
1. pipelining
2. growable hashtable
3. curious to learn about unaligned memeory.

test cases:
1. normal str
2. empty str
3. large str
4. figure out how to measure latency and throughput.

So "I/O bottleneck" basically means:
The CPU is sitting idle because it is waiting for something outside the CPU (network, disk, database, etc.) to finish.
The network waiting time is the big issue for latency.
The number of I/O events per second is the issue for throughput.
Those are related but different bottlenecks.
So the same word "I/O bottleneck" can mean:
slow I/O completion (waiting for network/disk)
too many I/O operations to process (event/syscall overhead)

// sizeof give \n, strlen without it
